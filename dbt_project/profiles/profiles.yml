customer:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost  # or the appropriate host for your database
      user: postgres
      pass: postgres   # Note that it should be 'pass', not 'password'
      port: 5432
      dbname: postgres
      schema: public
      threads: 1
rental:
  target: dev
  outputs:
    dev:
      type: postgres
      # host: dummy_host
      # user: dummy_user
      # pass: dummy_pass
      # port: 5432
      # dbname: dummy_db
      schema: public
      threads: 1